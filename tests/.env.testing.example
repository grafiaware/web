# Copy/export these when using docker-compose.test.yml
TEST_DB_HOST=127.0.0.1
TEST_DB_PORT=3307
TEST_DB_USER=webtest
TEST_DB_PASSWORD=webtest
TEST_DB_RED=web_red_test
TEST_DB_EVENTS=events_test
TEST_DB_AUTH=auth_test
# Set to 1 only after you import Red/Events/Auth schemas into the test databases:
# TEST_DB_SCHEMA_READY=1

# smtp4dev (docker-compose.test.yml)
# SMTP: localhost:25
# API / UI: http://localhost:5000
